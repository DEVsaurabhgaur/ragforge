version: "3.9"

services:
  ragforge:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./uploaded_docs:/app/uploaded_docs
      - ./.sessions:/app/.sessions
    restart: unless-stopped
